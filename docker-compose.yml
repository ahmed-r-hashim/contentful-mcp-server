# https://docs.docker.com/compose/environment-variables/
services:
  mcp-contentful-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3030:3030
    env_file:
      - .env # Load environment variables from .env file
 